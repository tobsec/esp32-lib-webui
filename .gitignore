build/
managed_components/
sdkconfig
sdkconfig.old

# OTA signing keys — 32-byte raw Ed25519 seeds. Pair with each consumer
# project's HK_OTA_SIGNING_PUBKEY. Loss = no more OTA updates for
# currently-deployed hardware until reflash, so back up offline.
.ota_signing_key
*.signing_key

.cache/
.idea/
.vscode/
.claude/
*.log
*.bak
.component_hash

# Python bytecode (pytest, ota_sign.py)
__pycache__/
*.pyc
